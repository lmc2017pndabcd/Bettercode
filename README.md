# Bettercode——A simple stupid agent for developers(including MCP,SKILL.md and default functions)
## What's up
I'm using Claude Code(An agent developed by Anthropic). But in fact,Claude Code's Tool-Calling is so strict.
I'm also using CodeX(Another agent created by OpenAI).But it SOMETIMES COSTS SO MUCH(for model) and its pet is annoying.
I don't like those things.So why don't I create one?
So I did this.

## How to use it?
Run this command:
```bash
make all
```
and run this:
```bash
./bet
```
(ADD IT TO $PATH!)
